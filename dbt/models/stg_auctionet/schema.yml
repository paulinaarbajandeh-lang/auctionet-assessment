version: 2

models:
  - name: stg_auctionet_items
    description: "Staging view that parses individual item attributes out into a clean relational format inside silver_space."
    columns:
      - name: item_id
        description: "Primary key identifier for the auction item."
        data_tests:
          - unique
          - not_null